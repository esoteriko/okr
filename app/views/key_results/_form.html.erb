<%= form_with(model: key_result, local: true) do |form| %>
  <% if key_result.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(key_result.errors.count, "error") %> prohibited this key_result from being saved:</h2>

      <ul>
      <% key_result.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :objective_id %>
    <%= form.text_field :objective_id %>
  </div>

  <div class="field">
    <%= form.label :kr_measure_id %>
    <%= form.text_field :kr_measure_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
