<center>
<%= form_with(model: group, local: true) do |form| %>
  <% if group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(group.errors.count, "error") %> prohibited this group from being saved:</h2>

      <ul>
      <% group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<center>

<h2><stronger>Grupos</stronger></h2>
    <div class="container">
    <div class="text-muted">Group Name</div>
    <%= form.text_field :name, class:"form-control", placeholder:"Nombre del grupo" %>
    </div>
<br>

  <div class="container">
    <div class="text-muted">Objetivo</div>
    <%=form.number_field :group_type_id, class:"form-control", placeholder:"Tipo de grupo"%>
  </div>
<br>
  <div class="actions">
    <%= form.submit "Guardar", class:"btn btn-primary" %>
     </div>
</center>
</div>
  
  
<% end %>
</center>